import React, { useState, useEffect } from 'react';
import { BrowserRouter as Router, Routes, Route, Link, useLocation, useParams, useNavigate } from 'react-router-dom';
import { Menu, X, Check, ChevronDown, ArrowRight, Monitor, Smartphone, Mail, Clock, ShieldCheck, ExternalLink } from 'lucide-react';
import { CONFIG } from './config';

// --- Reusable Components ---

const Button = ({ children, to, variant = 'primary', className = '', onClick }: any) => {
  const base = "px-6 py-3 rounded-md font-medium transition-all duration-200 inline-flex items-center justify-center gap-2";
  const styles = {
    primary: "bg-teal text-white hover:bg-teal/90",
    secondary: "bg-white text-navy border border-gray-200 hover:border-gray-400",
    outline: "border-2 border-teal text-teal hover:bg-teal hover:text-white"
  };
  
  if (to?.startsWith('#')) {
    return <a href={to} className={`${base} ${styles[variant as keyof typeof styles]} ${className}`}>{children}</a>;
  }
  if (to) {
    return <Link to={to} className={`${base} ${styles[variant as keyof typeof styles]} ${className}`}>{children}</Link>;
  }
  return <button onClick={onClick} className={`${base} ${styles[variant as keyof typeof styles]} ${className}`}>{children}</button>;
};

const SectionHeading = ({ eyebrow, title, description, light = false }: any) => (
  <div className="mb-12">
    {eyebrow && <span className={`text-sm font-bold tracking-[0.2em] uppercase mb-4 block ${light ? 'text-teal' : 'text-teal'}`}>{eyebrow}</span>}
    <h2 className={`text-3xl md:text-4xl font-bold mb-4 ${light ? 'text-white' : 'text-navy'}`}>{title}</h2>
    {description && <p className={`max-w-2xl text-lg ${light ? 'text-slate-300' : 'text-slate-600'}`}>{description}</p>}
  </div>
);

const DemoDisclosure = () => (
  <div className="bg-amber-50 border-b border-amber-100 text-amber-800 text-[10px] uppercase tracking-widest font-bold py-2 text-center fixed top-0 w-full z-[100]">
    Concept Project — Fictional Business
  </div>
);

// --- Main Pages ---

const Home = () => {
  const [activeFaq, setActiveFaq] = useState<number | null>(null);

  return (
    <div className="bg-white">
      {/* 1. Header */}
      <nav className="fixed w-full z-50 bg-white/80 backdrop-blur-md border-b border-gray-100 h-20 flex items-center">
        <div className="max-w-7xl mx-auto px-6 w-full flex justify-between items-center">
          <Link to="/" className="text-xl font-bold text-navy tracking-tight uppercase">Titus <span className="text-teal">Web Studio</span></Link>
          <div className="hidden md:flex gap-8 items-center">
            {['Work', 'Services', 'Pricing', 'About', 'Contact'].map(item => (
              <a key={item} href={`#${item.toLowerCase()}`} className="text-sm font-medium text-slate-600 hover:text-navy">{item}</a>
            ))}
            <Button to="#contact" variant="primary" className="text-sm py-2">Get a Free Quote</Button>
          </div>
        </div>
      </nav>

      {/* 2. Hero */}
      <section className="pt-40 pb-20 px-6 bg-navy text-white">
        <div className="max-w-7xl mx-auto grid lg:grid-cols-2 gap-16 items-center">
          <div>
            <span className="text-teal font-bold tracking-[0.3em] text-sm uppercase block mb-4">TITUS WEB STUDIO</span>
            <h1 className="text-5xl md:text-7xl font-bold leading-[1.1] mb-6">Professional Websites for Pressure Washing Businesses.</h1>
            <p className="text-xl text-slate-300 mb-10 max-w-lg">Clean, mobile-friendly websites designed to help customers explore your services and request a quote with ease.</p>
            <div className="flex flex-wrap gap-4">
              <Button to="#contact">Get a Free Quote</Button>
              <Button to="#work" variant="secondary">View My Work</Button>
            </div>
          </div>
          <div className="relative bg-slate-800 rounded-2xl p-4 border border-white/10 shadow-2xl">
             <div className="aspect-video bg-slate-900 rounded-lg flex items-center justify-center">
                <Monitor className="text-teal/20 w-32 h-32" />
             </div>
          </div>
        </div>
      </section>

      {/* 3. Featured Work */}
      <section id="work" className="py-24 px-6 bg-off-white">
        <div className="max-w-7xl mx-auto">
          <SectionHeading title="Selected Work" description="Thoughtful website concepts built for local service businesses." />
          <div className="grid md:grid-cols-3 gap-8">
            {CONFIG.projects.map(project => (
              <div key={project.id} className="bg-white rounded-xl overflow-hidden border border-gray-200 group hover:shadow-xl transition-all">
                <div className="h-52 bg-slate-200 flex items-center justify-center">
                   <span className="text-xs font-bold text-slate-400 uppercase tracking-widest">{project.name}</span>
                </div>
                <div className="p-8">
                  <span className="text-xs font-bold text-teal uppercase mb-2 block">{project.industry}</span>
                  <h3 className="text-xl font-bold mb-4">{project.name}</h3>
                  <div className="space-y-3">
                    <Button to={`/demo/${project.id}`} variant="primary" className="w-full text-sm">View Live Demo</Button>
                    <Button to={`/work/${project.id}`} variant="secondary" className="w-full text-sm">Explore Project</Button>
                  </div>
                  <p className="mt-6 text-[10px] text-gray-400 text-center uppercase font-bold tracking-tighter">Concept Project — Fictional Business</p>
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* 4. Services */}
      <section id="services" className="py-24 px-6">
        <div className="max-w-7xl mx-auto">
          <SectionHeading title="Web Design Built Around Your Business." />
          <div className="grid md:grid-cols-3 gap-12">
            {[
              { t: "Custom Website Design", d: "Modern, mobile-friendly websites tailored to your services, brand, and customers." },
              { t: "Website Redesign", d: "Give an outdated website a cleaner look and a more intuitive customer experience." },
              { t: "Quote-Focused Landing Pages", d: "Focused pages that make it easy for visitors to understand your offer and get in touch." }
            ].map((s, i) => (
              <div key={i} className="p-8 bg-off-white rounded-2xl">
                <h3 className="text-xl font-bold mb-4">{s.t}</h3>
                <p className="text-slate-600 leading-relaxed">{s.d}</p>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* 5. Why Titus */}
      <section className="py-24 px-6 bg-navy text-white text-center">
        <div className="max-w-4xl mx-auto">
          <SectionHeading light title="Designed With Your Customers in Mind." />
          <div className="grid md:grid-cols-3 gap-8 mt-12 text-left">
            {[
              { t: "Clean, Premium Design", d: "Make a professional first impression with a website that reflects the quality of your work." },
              { t: "Easy Quote Requests", d: "Help visitors find your services and reach out without unnecessary steps." },
              { t: "Support After Launch", d: "Get help with fixes and adjustments during the included post-launch period." }
            ].map((b, i) => (
              <div key={i}>
                <Check className="text-teal mb-4" />
                <h4 className="font-bold mb-2">{b.t}</h4>
                <p className="text-slate-400 text-sm">{b.d}</p>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* 6. Pricing */}
      <section id="pricing" className="py-24 px-6 bg-off-white">
        <div className="max-w-3xl mx-auto text-center">
          <SectionHeading title="Straightforward Website Pricing." />
          <div className="bg-white border border-gray-200 rounded-3xl p-8 md:p-12 shadow-sm mt-8">
            <h3 className="text-2xl font-bold text-navy mb-2">{CONFIG.business.pricing.package}</h3>
            <div className="text-5xl font-bold text-teal mb-8">Starting at ${CONFIG.business.pricing.price}</div>
            <ul className="text-left space-y-4 mb-10 max-w-md mx-auto">
              {["Up to 4 pages", "Custom responsive design", "Quote request form", "Project photo gallery", "Basic on-page SEO setup", "2 revision rounds", "14 days of support"].map(item => (
                <li key={item} className="flex items-center gap-3 text-slate-600"><Check size={18} className="text-teal" /> {item}</li>
              ))}
            </ul>
            <Button to="#contact" className="w-full py-4 text-lg">Request a Project Quote</Button>
            <p className="mt-6 text-xs text-slate-400">Timeline: {CONFIG.business.pricing.timeline}. Pricing varies by project requirements.</p>
          </div>
        </div>
      </section>

      {/* 7. How it Works */}
      <section className="py-24 px-6">
        <div className="max-w-7xl mx-auto">
          <SectionHeading title="A Simple Process From Start to Launch." />
          <div className="grid md:grid-cols-4 gap-8">
            {[
              { n: "01", t: "Tell Me About Your Business", d: "Share your services, goals, and the features you need." },
              { n: "02", t: "Design & Build", d: "I create a responsive website based on the agreed scope." },
              { n: "03", t: "Review & Refine", d: "Review the site and request changes within revision rounds." },
              { n: "04", t: "Launch & Support", d: "The site goes live, followed by 14 days of support." }
            ].map((step, i) => (
              <div key={i} className="relative">
                <span className="text-5xl font-black text-slate-100 absolute -top-8 -left-2 z-0">{step.n}</span>
                <div className="relative z-10">
                  <h4 className="font-bold mb-4">{step.t}</h4>
                  <p className="text-slate-600 text-sm leading-relaxed">{step.d}</p>
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* 8. About */}
      <section id="about" className="py-24 px-6 bg-off-white">
        <div className="max-w-4xl mx-auto grid md:grid-cols-2 gap-16 items-center">
          <div className="aspect-square bg-slate-200 rounded-2xl flex items-center justify-center">
             <span className="text-slate-400 font-bold uppercase tracking-[0.2em]">Titus</span>
          </div>
          <div>
            <h2 className="text-4xl font-bold text-navy mb-2">Hi, I'm Titus.</h2>
            <p className="text-teal font-bold mb-6">Freelance Web Designer</p>
            <div className="text-slate-600 space-y-4">
              <p>I'm a freelance web designer based in South Korea, working with businesses worldwide.</p>
              <p>I help small service businesses build clean, professional websites that make their services easy to understand and their contact information easy to find.</p>
              <p>My approach is simple: thoughtful design, clear communication, and a website built around what your business actually needs.</p>
            </div>
          </div>
        </div>
      </section>

      {/* 9. FAQ */}
      <section className="py-24 px-6">
        <div className="max-w-3xl mx-auto">
          <SectionHeading title="Frequently Asked Questions" />
          <div className="space-y-4">
            {[
              { q: "How much does a website cost?", a: `Pricing typically starts at $${CONFIG.business.pricing.price} for a 4-page responsive site.` },
              { q: "How long does it take?", a: `An average project takes ${CONFIG.business.pricing.timeline} after all materials are received.` },
              { q: "Are the websites mobile-friendly?", a: "Yes, every website I build is designed to look great on desktops, tablets, and smartphones." },
              { q: "How do I pay?", a: "Payments are handled securely via Payoneer. I typically require a 50% deposit to start." }
            ].map((faq, i) => (
              <div key={i} className="border border-gray-100 rounded-lg overflow-hidden">
                <button onClick={() => setActiveFaq(activeFaq === i ? null : i)} className="w-full px-6 py-4 text-left font-bold flex justify-between items-center hover:bg-off-white">
                  {faq.q} <ChevronDown className={`transition-transform ${activeFaq === i ? 'rotate-180' : ''}`} />
                </button>
                {activeFaq === i && <div className="px-6 py-4 text-slate-600 bg-off-white border-t border-gray-100">{faq.a}</div>}
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* 10. Contact */}
      <section id="contact" className="py-24 px-6 bg-navy text-white">
        <div className="max-w-7xl mx-auto grid lg:grid-cols-2 gap-20">
          <div>
            <h2 className="text-4xl font-bold mb-6">Let's Build Your Website.</h2>
            <p className="text-slate-400 mb-8 text-lg">Tell me about your business and the website you have in mind. I'll review your request and get back to you by email.</p>
            <div className="space-y-6">
              <div className="flex items-center gap-4 text-slate-300"><Mail className="text-teal" /> {CONFIG.business.email}</div>
              <div className="flex items-center gap-4 text-slate-300"><ShieldCheck className="text-teal" /> Professional, secure process</div>
            </div>
          </div>
          <ContactForm />
        </div>
      </section>

      {/* 11. Footer */}
      <footer className="py-12 border-t border-gray-100 text-center">
         <p className="text-sm text-slate-400">© {new Date().getFullYear()} {CONFIG.business.name}. All rights reserved.</p>
      </footer>
    </div>
  );
};

// --- Contact Form Logic ---
const ContactForm = () => {
  const [sent, setSent] = useState(false);
  const handleSubmit = (e: any) => {
    e.preventDefault();
    setSent(true); // Demonstrating validation/submission state
  };

  if (sent) return (
    <div className="bg-white/5 border border-white/10 p-10 rounded-2xl text-center">
      <Check size={48} className="text-teal mx-auto mb-6" />
      <h3 className="text-2xl font-bold mb-2">Inquiry Preview Mode</h3>
      <p className="text-slate-400">This is a demo. In the final version, this form will send an email to {CONFIG.business.email}.</p>
      <button onClick={() => setSent(false)} className="mt-6 text-teal font-bold underline">Reset Form</button>
    </div>
  );

  return (
    <form onSubmit={handleSubmit} className="space-y-6">
      <div className="grid md:grid-cols-2 gap-6">
        <input required placeholder="Name *" className="w-full bg-white/5 border border-white/10 rounded-lg px-4 py-3 outline-none focus:border-teal" />
        <input required type="email" placeholder="Email *" className="w-full bg-white/5 border border-white/10 rounded-lg px-4 py-3 outline-none focus:border-teal" />
      </div>
      <input placeholder="Business Name" className="w-full bg-white/5 border border-white/10 rounded-lg px-4 py-3 outline-none focus:border-teal" />
      <textarea required rows={4} placeholder="Project description *" className="w-full bg-white/5 border border-white/10 rounded-lg px-4 py-3 outline-none focus:border-teal"></textarea>
      <button className="w-full bg-teal text-white font-bold py-4 rounded-lg hover:bg-teal/90 transition-colors">Send My Project Inquiry</button>
    </form>
  );
};

// --- Case Study Page ---
const CaseStudy = () => {
  const { id } = useParams();
  const project = CONFIG.projects.find(p => p.id === id);
  if (!project) return <div>Project not found</div>;

  return (
    <div className="pt-32 pb-20 px-6">
      <div className="max-w-4xl mx-auto">
        <Link to="/" className="text-teal font-bold flex items-center gap-2 mb-8"><ArrowRight className="rotate-180" size={16} /> Back to All Work</Link>
        <span className="bg-amber-50 text-amber-800 text-[10px] font-bold px-3 py-1 rounded-full uppercase tracking-widest mb-4 inline-block">Concept Project</span>
        <h1 className="text-5xl font-bold text-navy mb-4">{project.name}</h1>
        <div className="flex gap-4 text-slate-400 mb-12"><span>{project.industry}</span><span>•</span><span>{project.location}</span></div>
        <div className="aspect-video bg-slate-100 rounded-2xl mb-12 flex items-center justify-center border border-gray-100">
           <Monitor size={64} className="text-slate-200" />
        </div>
        <div className="prose prose-lg mb-12 text-slate-600 space-y-6">
          <h2 className="text-2xl font-bold text-navy">Design Concept</h2>
          <p>{project.description}</p>
        </div>
        <Button to={`/demo/${project.id}`} className="w-full md:w-auto">View Live Demo</Button>
      </div>
    </div>
  );
};

// --- Demo Site 1: Lone Star ---
const DemoLoneStar = () => (
  <div className="bg-white min-h-screen">
    <DemoDisclosure />
    <nav className="pt-12 px-8 flex justify-between items-center max-w-7xl mx-auto">
      <div className="text-2xl font-black text-navy italic">LONE STAR <span className="text-teal">EXTERIOR</span></div>
      <button className="bg-teal text-white px-6 py-2 font-bold rounded-sm">FREE ESTIMATE</button>
    </nav>
    <header className="py-24 px-8 text-center bg-navy text-white mt-8">
      <h1 className="text-6xl font-black mb-6 uppercase italic">Bring Back The Clean.</h1>
      <p className="text-xl text-slate-400 mb-10">Professional Pressure Washing in Austin, TX.</p>
      <button className="bg-teal text-white px-10 py-4 font-bold text-lg">Request a Quote</button>
    </header>
    <div className="py-20 text-center text-slate-400 text-xs italic uppercase tracking-widest">
      Sample Testimonials — Fictional Content for Design Demonstration
    </div>
  </div>
);

// --- Demo Site 2: Clearview ---
const DemoClearview = () => (
  <div className="bg-off-white min-h-screen">
    <DemoDisclosure />
    <nav className="pt-12 px-8 flex justify-between items-center max-w-7xl mx-auto">
      <div className="text-xl font-bold text-blue-800">Clearview Pressure Washing</div>
      <div className="flex gap-6 text-sm font-medium"><span>Services</span><span>Gallery</span></div>
    </nav>
    <header className="py-32 px-8 text-center">
      <h1 className="text-5xl font-light text-navy mb-6">A Fresh Look for Every Surface.</h1>
      <button className="bg-blue-600 text-white px-8 py-3 rounded-full font-bold">Get Started</button>
    </header>
    <div className="py-20 text-center text-slate-400 text-xs italic uppercase tracking-widest">
      Sample Testimonials — Fictional Content for Design Demonstration
    </div>
  </div>
);

// --- Demo Site 3: Summit ---
const DemoSummit = () => (
  <div className="bg-[#262626] text-[#F5F5F7] min-h-screen">
    <DemoDisclosure />
    <nav className="pt-12 px-8 flex justify-between items-center max-w-7xl mx-auto">
      <div className="text-xl tracking-[0.3em] font-light">SUMMIT</div>
      <div className="text-sm border border-white/20 px-4 py-1">MENU</div>
    </nav>
    <header className="h-screen flex flex-col justify-center items-center text-center px-8">
      <h1 className="text-5xl md:text-7xl font-serif mb-8 italic">Protecting Your Property Starts at the Top.</h1>
      <p className="max-w-xl text-slate-400 mb-10 tracking-widest uppercase text-xs font-bold">Roof & Exterior Care Specialists | Denver, CO</p>
      <div className="w-12 h-[1px] bg-[#B45309] mb-10"></div>
    </header>
    <div className="py-20 text-center text-white/20 text-xs italic uppercase tracking-widest">
      Sample Testimonials — Fictional Content for Design Demonstration
    </div>
  </div>
);

// --- Main App Shell & Routing ---
export default function App() {
  return (
    <Router>
      <Routes>
        <Route path="/" element={<Home />} />
        <Route path="/work/:id" element={<CaseStudy />} />
        <Route path="/demo/lone-star" element={<DemoLoneStar />} />
        <Route path="/demo/clearview" element={<DemoClearview />} />
        <Route path="/demo/summit" element={<DemoSummit />} />
      </Routes>
    </Router>
  );
}